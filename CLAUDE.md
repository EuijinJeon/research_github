# CLAUDE.md

MLP 해석가능성 프로토콜 재현 프로젝트. **목적은 새 발견이 아니라 개념 이해와 선행연구 재현.**

## 먼저 읽을 것

- `docs/open_questions.md` — **현재 위치, 다음 할 일, 미결정 사항.** 세션 시작 시 여기부터.
- `docs/walkthrough_stage1.md` — 개념을 한 걸음씩 (설명이 필요할 때의 기준 톤)
- `docs/stage1_log.md` — 예측 P1~P6과 실측 결과

## 작업 규칙 (사용자가 명시한 것)

1. **각 단계 전에** 무엇을·왜·어떤 결과를 예상하는지 설명하고 확인을 받는다. 임의로 다음 단계로 넘어가지 않는다.
2. 코드는 한 번에 한 파일씩. 긴 파일은 함수 단위로 끊어 설명하며 작성.
3. 새 개념은 **코드보다 먼저** 설명한다. 모른다고 가정해도 된다.
4. 각 단계 끝에 "이 단계에서 알게 된 것"을 한 문단으로.
5. **결과가 예상과 다르면 넘어가지 말고 왜 다른지 파고든다.**
6. 중간 산출물은 디스크에 저장 — 뒷단계 재실행 시 앞단계를 반복하지 않게.
7. 속도보다 이해가 우선.

**설명 톤:** 스텝 간 갭을 최소화하고, self-contained하게, 직관적으로.
추상적 정의만 던지지 말고 **손으로 따라갈 수 있는 작은 숫자 예시**를 먼저 보여줄 것.
가능하면 그 숫자를 실제로 출력하는 스크립트를 함께 만든다 (`scripts/walkthrough.py`가 그 예).

**검증 규칙:** 실험 전에 **예측을 문서에 먼저 적고** 실행한다. 대조군을 함께 둔다
(예: P6에서 꺾임점 vs 비꺾임점 거리 15배 분리 — 허용치를 느슨하게 줘 통과한 게 아님을 보임).

## 환경

- 주력 RTX 4060 (Linux) / 보조 M4 Pro. 코드는 device-agnostic.
- Python 3.12 (uv). **시스템 파이썬은 3.14 하나뿐이니 절대 건드리지 말 것.**
- MPS: float64 미지원, `torch.compile` 불안정 → 둘 다 쓰지 않는다.
- `MLPINTERP_DEVICE=cpu` 로 device 강제 가능 (맥 탈출구).
- wandb는 offline (`WANDB_MODE=offline`). Stage 3 전까지는 아예 쓰지 않음.

## 코드 관례

- 중간 산출물은 `artifacts/` 아래. **활성 패턴이 정본, `A_r`은 파생물** (용량 때문).
- 체크포인트는 CPU 텐서로 저장하고 로드 시 `map_location` 명시.
- 클러스터링/거리는 반드시 **증강 행렬 `[A_r | b_r]`** 위에서. `A_r`만 쓰면 절편이 다른
  서로 다른 함수를 합쳐버린다.
